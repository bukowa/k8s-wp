apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: "http"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
spec:
  rules:
    - host: {{ .Values.web.host }}
      http:
        paths:
          - backend:
              service:
                port:
                  name: "http"
                name: {{ .Release.Name }}
            pathType: Prefix
            path: /
